{
  "name": "automated-email-ticket-rating",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix feedback-website",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix feedback-website",
    "build": "npm run build:backend && npm run build:frontend",
    "start": "node backend/dist/server.js",
    "install:all": "npm install --prefix backend && npm install --prefix feedback-website"
  }
}

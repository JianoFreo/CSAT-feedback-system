{
  "name": "csat-feedback-system",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix rating",
    "start": "npm run start --prefix backend"
  }
}
